#include <iostream>
#include <cctype>
#include <unordered_map>
#include <vector>
#include <chrono>

unsigned int part1();
unsigned int part2();
unsigned int read_numbers_until_iteration(std::size_t last_iteration);